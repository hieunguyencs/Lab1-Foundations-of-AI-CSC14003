BẢN ĐỒ THƯỜNG

- BFS, DFS giống nhau

- UCS chậm hơn BFS, DFS. Không thích hợp với đồ thị không có trọng số như thế này, vì việc lấy min g() không có ý nghĩa, mà lại tốn chi phí log() cho heap. 

- Heuristic 1: h1(x, y) = Khoảng cách manhattan từ (x, y) đến exit
	+ A*: 
	+ GBFS: 

- Heuristic 2: h2(x, y) = Khoảng cách đường chim bay từ (x, y) đến exit
	+ A* 
	+ GBFS: 

BẢN ĐỒ ĐIỂM THƯỞNG

BẢN ĐỒ ĐIỂM ĐÓN

BẢN ĐỒ DỊCH CHUYỂN